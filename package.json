{
  "name": "termrail",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "start": "npm run dev",
    "dev": "npm run build --workspace @termrail/shared && concurrently -n server,web -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "dev:server": "npm run build --workspace @termrail/shared && npm run dev --workspace server",
    "dev:web": "npm run build --workspace @termrail/shared && npm run dev --workspace web",
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2"
  }
}
